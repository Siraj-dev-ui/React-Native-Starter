import {View, Text} from 'react-native';
import React from 'react';
import Header from '../../components/Header';

const DaakScreen = () => {
  return (
    <View>
      <Header text={'Daak'} />
    </View>
  );
};

export default DaakScreen;
